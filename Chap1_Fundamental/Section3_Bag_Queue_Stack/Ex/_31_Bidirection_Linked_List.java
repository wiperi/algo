package Chap1_Fundamental.Section3_Bag_Queue_Stack.Ex;

/**
 * 1.3.31 实现一个嵌套类DoubleNode 用来构造双向链表，其中每个结点都含有一个指向前驱元素的引用
 * 和一项指向后续元素的引用（如果不存在则为null）。为以下任务实现若干静态方法：在表头
 * 插入结点、在表尾插入结点、从表头删除结点、从表尾删除结点、在指定结点之前插入新结点、
 * 在指定结点之后插入新结点、删除指定结点。
 */
public class _31_Bidirection_Linked_List<Item> {
    class DoubleNode {
        Item item;
        DoubleNode next;
        DoubleNode pre;
    }

    /* 
     * addHead
     * addTail
     * removeHead
     * removeTail
     * insert
     * delete
     */
}
